## Summary

- Updated `git_push` and `git_pull` functions to use `gh auth setup-git` for authentication
- No SSH keys required - uses GH_TOKEN for authentication
- Enhanced system prompt to clarify gh CLI usage for GitHub operations

## Changes

1. **agent/tools/git.py**:
   - Added `_run_gh_auth_setup()` function to configure git credential helper via gh CLI
   - Modified `git_push()` to call `_run_gh_auth_setup()` before push
   - Modified `git_pull()` to call `_run_gh_auth_setup()` before pull
   - Updated docstrings to clarify gh CLI authentication usage

2. **agent/core/agent.py**:
   - Updated system prompt with note about gh CLI authentication

## Testing

- All tests pass (`pytest tests/test_cli.py`)
- Tested git push via run_agent_subprocess - works correctly
- gh CLI authentication works with GH_TOKEN (no SSH required)
